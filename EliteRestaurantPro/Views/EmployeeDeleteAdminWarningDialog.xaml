<Window x:Class="EliteRestaurantPro.Views.EmployeeDeleteAdminWarningDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Delete employee"
        Width="480"
        SizeToContent="Height"
        MinHeight="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border CornerRadius="14"
            Background="{StaticResource BackgroundDarkBrush}"
            BorderBrush="{StaticResource BorderSubtleBrush}"
            BorderThickness="1"
            ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="6" Direction="270" Color="#99000000" Opacity="0.8"/>
        </Border.Effect>
        <Border Style="{StaticResource GlassCard}" Margin="20" Padding="28,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="Transparent"
                        Padding="0,0,0,4"
                        Cursor="Hand"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <TextBlock x:Name="TitleText" Style="{StaticResource HeadingMedium}"/>
                </Border>

                <TextBlock x:Name="WarningText"
                           Grid.Row="1"
                           Style="{StaticResource BodyText}"
                           TextWrapping="Wrap"
                           LineHeight="20"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,20"/>

                <StackPanel Grid.Row="2" Orientation="Vertical">
                    <Button x:Name="DeleteAnywayButton"
                            Style="{StaticResource GoldPrimaryButton}"
                            MinWidth="280"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,10"
                            Click="DeleteAnyway_Click"/>
                    <Button x:Name="BackButton"
                            Style="{StaticResource GhostButton}"
                            MinWidth="280"
                            HorizontalAlignment="Stretch"
                            IsCancel="True"
                            Click="Back_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>
